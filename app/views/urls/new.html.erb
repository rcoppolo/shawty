<h2>Enter a URL to be shortened</h2>

<%= form_for(@url) do |f| %>
	<p>
		<%= f.label :long_url %>
		<%= f.text_field :long_url %>
		<% if user_signed_in? %>
			<%= f.hidden_field :user_id, :value => current_user.id  %>
		<% end %>
	</p>
	
	<%= f.submit 'shorten' %>
	
<% end %>